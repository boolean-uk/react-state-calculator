// add onClick to numberButton '1'

// take the value and store it in a string

// add onClick to symbolButton '+'

// take the value and store it in a string

// add onClick to numberScndButton '1'

// take the value and store it in a string

// add onClick to equalButton

// using the strings, calculate the result
